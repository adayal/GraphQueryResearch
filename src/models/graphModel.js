'use strict';


//var cypher = require('node-cypher');
export default class Graph {
	constructor() {
	}
	static fetchGraph(callback) {
	/*	
		cypher.createClient('http://localhost:7474' , function(err,clinet) {
				client.query("MATCH (n: ) RETURN n", {}, function(err, result) {
					callback(result);
			});
		});
	*/
	}

}
